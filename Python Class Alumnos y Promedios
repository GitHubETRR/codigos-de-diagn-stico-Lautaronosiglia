class Persona:
    def __init__(self, nombre, edad):
        self.nombre = nombre
        self.edad = edad


class Alumno(Persona):   # Alumno hereda de Persona
    def __init__(self, nombre, edad):
        super().__init__(nombre, edad)  # Llama al constructor de Persona
        self.notas = []

    def agregar_nota(self, nota):
        self.notas.append(nota)

    def promedio(self):
        if len(self.notas) == 0:
            return 0
        return sum(self.notas) / len(self.notas)

    def mostrar_info(self):
        print("\n--- INFORMACIÓN DEL ALUMNO ---")
        print("Nombre:", self.nombre)
        print("Edad:", self.edad)
        print("Notas:", self.notas)
        print("Promedio:", round(self.promedio(), 2))


def main():
    nombre = input("Ingrese el nombre del alumno: ")
    edad = int(input("Ingrese la edad del alumno: "))

    alumno = Alumno(nombre, edad)

    while True:
        nota = input("Ingrese una nota (o escriba 'fin' para terminar): ")

        if nota.lower() == "fin":
            break

        alumno.agregar_nota(float(nota))

    alumno.mostrar_info()


if __name__ == "__main__":
    main()
